'use strict';
module.exports = {
		
		all: {
	        options: {
	            bases: ['<%= vars.buildPath %>'],
	            port: 8080,
	            hostname: "localhost",
	            livereload: true
	        }
	    }
};